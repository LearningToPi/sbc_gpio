/dts-v1/;
/plugin/;

/ {

	fragment@0 {
		target-path = "/";
		__overlay__ {
			{{ driver }}-{{ gpio_number }} {
				compatible = "{{ driver }}";
				status = "okay";
				gpios = <&gpio{{ gpio_chip|default(0) }} {{ gpio_pin }} {{ gpio_pin_ctrl|default(2) }}>;
			};
		};
	};
};
